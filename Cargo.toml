[package]
name = "stellar-xdr"
version = "0.0.0"
edition = "2021"

[dependencies]
base64 = "0.13.0"
serde = {version = "1.0.139", features = ["derive"], optional = true }

[features]
default = ["std"]
std = ["alloc", "base64/std"]
serde = ["alloc", "dep:serde"]
alloc = []
next = []
