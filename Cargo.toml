[package]
name = "stellar-xdr"
version = "0.0.0"
edition = "2021"

[dependencies]
base64 = "0.13.0"
serde_crate = {package = "serde", version = "1.0.137", features = ["derive"] }

[features]
default = ["std"]
std = ["alloc", "base64/std"]
alloc = []
next = []
serde = ["alloc"]
