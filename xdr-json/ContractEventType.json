{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "ContractEventType",
  "description": "ContractEventType is an XDR Enum defined as:\n\n```text enum ContractEventType { SYSTEM = 0, CONTRACT = 1, DIAGNOSTIC = 2 }; ```",
  "type": "string",
  "enum": [
    "system",
    "contract",
    "diagnostic"
  ]
}