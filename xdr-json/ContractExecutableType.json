{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "ContractExecutableType",
  "description": "ContractExecutableType is an XDR Enum defined as:\n\n```text enum ContractExecutableType { CONTRACT_EXECUTABLE_WASM = 0, CONTRACT_EXECUTABLE_STELLAR_ASSET = 1 }; ```",
  "type": "string",
  "enum": [
    "wasm",
    "stellar_asset"
  ]
}