{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "ExtendFootprintTtlResultCode",
  "description": "ExtendFootprintTtlResultCode is an XDR Enum defined as:\n\n```text enum ExtendFootprintTTLResultCode { // codes considered as \"success\" for the operation EXTEND_FOOTPRINT_TTL_SUCCESS = 0,\n\n// codes considered as \"failure\" for the operation EXTEND_FOOTPRINT_TTL_MALFORMED = -1, EXTEND_FOOTPRINT_TTL_RESOURCE_LIMIT_EXCEEDED = -2, EXTEND_FOOTPRINT_TTL_INSUFFICIENT_REFUNDABLE_FEE = -3 }; ```",
  "type": "string",
  "enum": [
    "success",
    "malformed",
    "resource_limit_exceeded",
    "insufficient_refundable_fee"
  ]
}