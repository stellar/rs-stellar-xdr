{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "ScSpecUdtDefKind",
  "description": "ScSpecUdtDefKind is an XDR Enum defined as:\n\n```text enum SCSpecUDTDefKind { SC_SPEC_UDT_DEF_LIB = 0, SC_SPEC_UDT_DEF_HASH = 1 }; ```",
  "type": "string",
  "enum": [
    "lib",
    "hash"
  ]
}