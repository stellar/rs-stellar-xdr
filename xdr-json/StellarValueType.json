{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "StellarValueType",
  "description": "`StellarValueType` is an XDR Enum defined as:\n\n```text enum StellarValueType { STELLAR_VALUE_BASIC = 0, STELLAR_VALUE_SIGNED = 1 #ifdef CAP_0083 , STELLAR_VALUE_EMPTY_TX_SET = 2 #endif }; ```",
  "type": "string",
  "enum": [
    "basic",
    "signed"
  ]
}