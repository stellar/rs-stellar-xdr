{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "SurveyMessageResponseType",
  "description": "SurveyMessageResponseType is an XDR Enum defined as:\n\n```text enum SurveyMessageResponseType { SURVEY_TOPOLOGY_RESPONSE_V2 = 2 }; ```",
  "type": "string",
  "enum": [
    "survey_topology_response_v2"
  ]
}