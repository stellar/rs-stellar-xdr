{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "Auth",
  "description": "Auth is an XDR Struct defined as:\n\n```text struct Auth { int flags; }; ```",
  "type": "object",
  "required": [
    "flags"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "flags": {
      "type": "integer",
      "format": "int32"
    }
  },
  "unevaluatedProperties": false
}