{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "LedgerKeyClaimableBalance",
  "description": "LedgerKeyClaimableBalance is an XDR NestedStruct defined as:\n\n```text struct { ClaimableBalanceID balanceID; } ```",
  "type": "object",
  "required": [
    "balance_id"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "balance_id": {
      "$ref": "#/definitions/ClaimableBalanceId"
    }
  },
  "unevaluatedProperties": false,
  "definitions": {
    "ClaimableBalanceId": {
      "type": "string"
    }
  }
}