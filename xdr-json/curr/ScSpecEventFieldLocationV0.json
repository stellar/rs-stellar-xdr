{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "ScSpecEventFieldLocationV0",
  "description": "ScSpecEventFieldLocationV0 is an XDR Enum defines as:\n\n```text enum SCSpecEventFieldLocationV0 { SC_SPEC_EVENT_FIELD_LOCATION_DATA = 0, SC_SPEC_EVENT_FIELD_LOCATION_TOPIC_LIST = 1 }; ```",
  "type": "string",
  "enum": [
    "data",
    "topic_list"
  ]
}