{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "ScpStatementType",
  "description": "ScpStatementType is an XDR Enum defines as:\n\n```text enum SCPStatementType { SCP_ST_PREPARE = 0, SCP_ST_CONFIRM = 1, SCP_ST_EXTERNALIZE = 2, SCP_ST_NOMINATE = 3 }; ```",
  "type": "string",
  "enum": [
    "prepare",
    "confirm",
    "externalize",
    "nominate"
  ]
}