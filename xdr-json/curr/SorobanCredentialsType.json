{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "SorobanCredentialsType",
  "description": "SorobanCredentialsType is an XDR Enum defines as:\n\n```text enum SorobanCredentialsType { SOROBAN_CREDENTIALS_SOURCE_ACCOUNT = 0, SOROBAN_CREDENTIALS_ADDRESS = 1 }; ```",
  "type": "string",
  "enum": [
    "source_account",
    "address"
  ]
}