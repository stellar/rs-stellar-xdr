{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "ArchivalProofType",
  "description": "ArchivalProofType is an XDR Enum defines as:\n\n```text enum ArchivalProofType { EXISTENCE = 0, NONEXISTENCE = 1 }; ```",
  "type": "string",
  "enum": [
    "existence",
    "nonexistence"
  ]
}