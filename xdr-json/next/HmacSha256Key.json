{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "HmacSha256Key",
  "description": "HmacSha256Key is an XDR Struct defined as:\n\n```text struct HmacSha256Key { opaque key[32]; }; ```",
  "type": "object",
  "required": [
    "key"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "key": {
      "type": "array",
      "items": {
        "type": "integer",
        "format": "uint8",
        "minimum": 0.0
      },
      "maxItems": 32,
      "minItems": 32
    }
  },
  "unevaluatedProperties": false
}