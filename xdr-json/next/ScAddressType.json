{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "ScAddressType",
  "description": "ScAddressType is an XDR Enum defines as:\n\n```text enum SCAddressType { SC_ADDRESS_TYPE_ACCOUNT = 0, SC_ADDRESS_TYPE_CONTRACT = 1 }; ```",
  "type": "string",
  "enum": [
    "account",
    "contract"
  ]
}